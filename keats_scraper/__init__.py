"""KEATS Student Handbook scraper feeding the RAG pipeline.

The package version is declared on the CLI entry point in main.py
(main.__version__) to keep a single source of truth for the
python main.py --version output. Nothing external consumes
keats_scraper.__version__, so the package itself does not re-export
one.
"""
